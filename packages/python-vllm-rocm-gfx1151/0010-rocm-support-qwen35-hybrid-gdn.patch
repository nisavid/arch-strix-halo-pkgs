diff --git i/vllm/config/vllm.py w/vllm/config/vllm.py
index bc1ce80..d589da9 100644
--- i/vllm/config/vllm.py
+++ w/vllm/config/vllm.py
@@ -1085,6 +1085,17 @@ class VllmConfig:
             )
         current_platform.check_and_update_config(self)
 
+        if self.model_config is not None and self.model_config.is_hybrid:
+            # Re-run hybrid alignment after platform-specific config updates.
+            # ROCm AITER can raise the KV cache block size after the first
+            # hybrid pass, and Qwen3.5/GDN needs the hybrid mamba alignment to
+            # preserve that platform minimum.
+            from vllm.model_executor.models.config import (
+                HybridAttentionMambaModelConfig,
+            )
+
+            HybridAttentionMambaModelConfig.verify_and_update_config(self)
+
         # Re-compute compile ranges after platform-specific config updates
         # (e.g., XPU may lower max_num_batched_tokens when MLA is enabled)
         self._set_compile_ranges()
diff --git i/vllm/model_executor/layers/fla/ops/chunk_delta_h.py w/vllm/model_executor/layers/fla/ops/chunk_delta_h.py
index ce60ca4..297d25c 100644
--- i/vllm/model_executor/layers/fla/ops/chunk_delta_h.py
+++ w/vllm/model_executor/layers/fla/ops/chunk_delta_h.py
@@ -14,7 +14,7 @@ from vllm.triton_utils import tl, triton
 
 from .index import prepare_chunk_indices, prepare_chunk_offsets
 from .op import exp
-from .utils import use_cuda_graph
+from .utils import is_amd, use_cuda_graph
 
 NUM_WARPS = [2, 4, 8, 16]
 
@@ -33,8 +33,12 @@ NUM_WARPS = [2, 4, 8, 16]
     configs=[
         triton.Config({"BV": BV}, num_warps=num_warps, num_stages=num_stages)
         for num_warps in [2, 4]
-        for num_stages in [2, 3, 4]
-        for BV in [32, 64]
+        # NOTE(gfx1151): On AMD HIP, restrict autotune search to
+        # num_stages=2 and BV=32. Higher stages and BV=64 with K>64
+        # produce code that causes GPU page faults during autotuning on
+        # RDNA 3.5 (wave32).
+        for num_stages in ([2] if is_amd else [2, 3, 4])
+        for BV in ([32] if is_amd else [32, 64])
     ],
     key=["H", "K", "V", "BT"],
     use_cuda_graph=use_cuda_graph,
@@ -190,8 +194,12 @@ def chunk_gated_delta_rule_fwd_kernel_h_blockdim64(
                 g + bos * H + i_h, (T,), (H,), (i_t * BT,), (BT,), (0,)
             )
             b_g = tl.load(p_g, boundary_check=(0,))
-            b_v = b_v * tl.where(m_t, exp(b_g_last - b_g), 0)[:, None]
-            b_g_last = exp(b_g_last)
+            # NOTE(gfx1151): Triton HIP fails to infer types for
+            # exp(scalar_bf16 - block_ptr_load_bf16). Cast to float32
+            # first, which also improves precision for exp computation.
+            b_g_diff = b_g_last.to(tl.float32) - b_g.to(tl.float32)
+            b_v = b_v * tl.where(m_t, exp(b_g_diff), 0)[:, None]
+            b_g_last = exp(b_g_last.to(tl.float32))
             b_h1 *= b_g_last
             if K > 64:
                 b_h2 *= b_g_last
diff --git i/vllm/model_executor/layers/fla/ops/chunk_o.py w/vllm/model_executor/layers/fla/ops/chunk_o.py
index 1307812..8c7957d 100644
--- i/vllm/model_executor/layers/fla/ops/chunk_o.py
+++ w/vllm/model_executor/layers/fla/ops/chunk_o.py
@@ -16,7 +16,7 @@ from vllm.triton_utils import tl, triton
 
 from .index import prepare_chunk_indices
 from .op import exp
-from .utils import FLA_GDN_FIX_BT, check_shared_mem, is_nvidia_hopper
+from .utils import FLA_GDN_FIX_BT, check_shared_mem, is_amd, is_nvidia_hopper
 
 BKV_LIST = [64, 128] if check_shared_mem() else [32, 64]
 NUM_WARPS = [2, 4] if is_nvidia_hopper else [2, 4, 8]
@@ -31,10 +31,10 @@ NUM_WARPS = [2, 4] if is_nvidia_hopper else [2, 4, 8]
 @triton.autotune(
     configs=[
         triton.Config({"BK": BK, "BV": BV}, num_warps=num_warps, num_stages=num_stages)
-        for BK in BKV_LIST
-        for BV in BKV_LIST
+        for BK in ([32] if is_amd else BKV_LIST)
+        for BV in ([32] if is_amd else BKV_LIST)
         for num_warps in NUM_WARPS
-        for num_stages in [2, 3, 4]
+        for num_stages in ([2] if is_amd else [2, 3, 4])
     ],
     key=["H", "K", "V", "BT"],
 )
diff --git i/vllm/model_executor/layers/mamba/gdn_linear_attn.py w/vllm/model_executor/layers/mamba/gdn_linear_attn.py
index 55cd17f..a445e51 100644
--- i/vllm/model_executor/layers/mamba/gdn_linear_attn.py
+++ w/vllm/model_executor/layers/mamba/gdn_linear_attn.py
@@ -584,8 +584,10 @@ class GatedDeltaNetAttention(PluggableLayer, MambaBase):
         Most kernels use a fixed ``BT = chunk_size`` (64), but
         ``chunk_fwd_kernel_o`` recomputes ``BT`` from the sequence
         length: ``min(64, max(16, next_power_of_2(T)))``.  Since ``BT``
-        is part of its autotune key, we run warmup passes with T = 16,
-        32, and 64 to cover all possible ``BT`` values.
+        is part of its autotune key. On AMD HIP, keep warmup at
+        T = 64 only because the Triton kernels use block pointers with
+        BT = 64, and HIP can fault on out-of-bounds address computation
+        for smaller warmup tensors even when boundary checks mask loads.
 
         The decode path uses ``fused_sigmoid_gating_delta_rule_update``
         which has fixed kernel parameters (no autotuning), so only the
@@ -601,11 +603,13 @@ class GatedDeltaNetAttention(PluggableLayer, MambaBase):
         num_v_heads = self.num_v_heads // self.tp_size
         _, state_dtype = self.get_state_dtype()
 
-        # Run warmup for each possible BT value of chunk_fwd_kernel_o:
-        #   T=16 → BT=16, T=32 → BT=32, T=64 → BT=64.
-        # Other kernels always use BT=chunk_size(64), so their autotune
-        # cache is populated on the first pass and reused thereafter.
-        for T in (16, 32, 64):
+        # NOTE(gfx1151): On RDNA 3.5 (wave32), the FLA Triton kernels
+        # use tl.make_block_ptr with BT=64 (chunk_size) and boundary_check
+        # for rows where T < BT. On HIP, the GPU page-faults on the address
+        # computation for the (BT, BK) block even though the actual load
+        # would be masked. T must be >= chunk_size (64) to avoid
+        # out-of-bounds address calculation.
+        for T in (64,):
             q = torch.randn(
                 1, T, num_k_heads, self.head_k_dim, device=device, dtype=dtype
             )
diff --git i/vllm/model_executor/models/config.py w/vllm/model_executor/models/config.py
index c13ea53..e59bede 100644
--- i/vllm/model_executor/models/config.py
+++ w/vllm/model_executor/models/config.py
@@ -215,7 +215,10 @@ class HybridAttentionMambaModelConfig(VerifyAndUpdateConfig):
                 dtype=kv_cache_dtype,
             ).page_size_bytes
         else:
-            kernel_block_alignment_size = 16
+            # If the platform already set a minimum block_size, preserve it.
+            # ROCm AITER sets block_size=64, and hybrid/GDN alignment must
+            # produce an attention block size that remains compatible with it.
+            kernel_block_alignment_size = max(16, cache_config.block_size)
             attn_page_size_1_token = FullAttentionSpec(
                 block_size=1,
                 num_kv_heads=model_config.get_num_kv_heads(parallel_config),
diff --git i/vllm/platforms/rocm.py w/vllm/platforms/rocm.py
index d3836a4..2e77d40 100644
--- i/vllm/platforms/rocm.py
+++ w/vllm/platforms/rocm.py
@@ -308,6 +308,7 @@ def flash_attn_triton_available() -> bool:
 def _get_backend_priorities(
     use_mla: bool,
     use_sparse: bool,
+    is_hybrid: bool = False,
 ) -> list[AttentionBackendEnum]:
     from vllm._aiter_ops import is_aiter_found_and_supported, rocm_aiter_ops
 
@@ -329,9 +330,12 @@ def _get_backend_priorities(
     backends = [
         AttentionBackendEnum.ROCM_ATTN,
     ]
-    if rocm_aiter_ops.is_mha_enabled():
+    # Hybrid models need TRITON_ATTN for full-attention layers because
+    # Qwen3.5/GDN can choose non-power-of-2 block sizes from mamba state
+    # alignment, while AITER attention kernels assume power-of-2 tiles.
+    if not is_hybrid and rocm_aiter_ops.is_mha_enabled():
         backends.append(AttentionBackendEnum.ROCM_AITER_FA)
-    if is_aiter_found_and_supported():
+    if not is_hybrid and is_aiter_found_and_supported():
         backends.append(AttentionBackendEnum.ROCM_AITER_UNIFIED_ATTN)
     backends.append(AttentionBackendEnum.TRITON_ATTN)
 
@@ -393,9 +397,19 @@ class RocmPlatform(Platform):
         valid_backends_priorities = []
         invalid_reasons = {}
 
+        from vllm.config import get_current_vllm_config_or_none
+
+        vllm_config = get_current_vllm_config_or_none()
+        _is_hybrid = bool(
+            vllm_config is not None
+            and vllm_config.model_config is not None
+            and vllm_config.model_config.is_hybrid
+        )
+
         backend_priorities = _get_backend_priorities(
             attn_selector_config.use_mla,
             attn_selector_config.use_sparse,
+            _is_hybrid,
         )
         for priority, backend in enumerate(backend_priorities):
             try:
@@ -425,6 +439,25 @@ class RocmPlatform(Platform):
 
         # First try checking just the selected backend, if there is one.
         if selected_backend is not None:
+            from vllm.config import get_current_vllm_config_or_none
+
+            vllm_config = get_current_vllm_config_or_none()
+            _is_hybrid = bool(
+                vllm_config is not None
+                and vllm_config.model_config is not None
+                and vllm_config.model_config.is_hybrid
+            )
+            if _is_hybrid and selected_backend in (
+                AttentionBackendEnum.ROCM_AITER_FA,
+                AttentionBackendEnum.ROCM_AITER_UNIFIED_ATTN,
+            ):
+                raise ValueError(
+                    "Selected AITER attention backend is not valid for "
+                    "hybrid models. Hybrid models need TRITON_ATTN for "
+                    "full-attention layers because their mamba alignment can "
+                    "choose non-power-of-2 block sizes."
+                )
+
             try:
                 backend_class = selected_backend.get_class()
                 invalid_reasons = backend_class.validate_configuration(
diff --git i/vllm/v1/attention/backends/rocm_aiter_unified_attn.py w/vllm/v1/attention/backends/rocm_aiter_unified_attn.py
index bd7f137..6989e40 100644
--- i/vllm/v1/attention/backends/rocm_aiter_unified_attn.py
+++ w/vllm/v1/attention/backends/rocm_aiter_unified_attn.py
@@ -40,7 +40,7 @@ class RocmAiterUnifiedAttentionBackend(RocmAttentionBackend):
     def supports_block_size(cls, block_size: int | None) -> bool:
         if block_size is None:
             return True
-        return block_size % 16 == 0
+        return block_size % 16 == 0 and (block_size & (block_size - 1)) == 0
 
     @classmethod
     def supports_head_size(cls, head_size: int) -> bool:
