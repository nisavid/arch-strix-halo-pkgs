{
  "maintenance": {
    "advisory_references": [
      "aur/aocl"
    ],
    "authoritative_reference": "none",
    "divergence_notes": [
      "There is no standalone Arch-family AOCL-LibM package, so this package is recipe-first and uses the broader aur/aocl bundle only for install-layout and metadata expectations.",
      "The package keeps AOCL-LibM out of CPython's own link line; it is intended as a downstream math library, not part of Python's PGO training path."
    ],
    "source_patches": [
      "0001-scons-support-arch-amdclang-toolchain.patch"
    ],
    "update_notes": [
      "When updating, audit upstream AOCL-LibM licensing and bundled notices directly from source rather than assuming the advisory aur/aocl metadata is sufficient.",
      "If AOCL-Utils packaging changes, re-check the runtime dependency and any RPATH or SONAME assumptions here.",
      "Keep the SCons compatibility carry as a source patch file instead of inline sed edits."
    ]
  },
  "name": "aocl-libm-gfx1151",
  "package_name": "aocl-libm-gfx1151",
  "pkgver": "5.2.2",
  "policy": {
    "advisory_references": [
      "aur/aocl"
    ],
    "arch_reference": [
      "aur/aocl"
    ],
    "authoritative_reference": "none",
    "compiler_dep": "rocm-llvm-gfx1151",
    "conflicts": [
      "aocl-libm"
    ],
    "depends": [
      "aocl-utils-gfx1151",
      "gcc-libs",
      "glibc"
    ],
    "divergence_notes": [
      "There is no standalone Arch-family AOCL-LibM package, so this package is recipe-first and uses the broader aur/aocl bundle only for install-layout and metadata expectations.",
      "The package keeps AOCL-LibM out of CPython's own link line; it is intended as a downstream math library, not part of Python's PGO training path."
    ],
    "install_prefix": "/usr",
    "license": [
      "BSD-3-Clause",
      "MIT",
      "Apache-2.0"
    ],
    "makedepends": [
      "git",
      "patchelf",
      "python",
      "rocm-llvm-gfx1151",
      "scons"
    ],
    "patches_dir": "packages/aocl-libm-gfx1151",
    "pkgdesc": "AMD AOCL-LibM rebuilt with Zen 5 tuning for Strix Halo gfx1151",
    "provides": [
      "aocl-libm"
    ],
    "recipe_key": "aocl_libm",
    "scaffold_notes": [
      "There is no standalone AOCL-LibM package in Arch, CachyOS, or AUR; the closest packaging lane is the broader AUR aocl bundle, which is only advisory for install-layout cleanup patterns.",
      "The recipe carries three upstream-compatibility source edits; the package keeps them as a source patch file, plus a post-install RPATH fix in package().",
      "Use Arch's system scons from makedepends directly. Do not recreate the recipe's venv-local pip bootstrap inside PKGBUILD; that makes package builds network-dependent.",
      "Pass resolved compiler paths to SCons through ALM_CC and ALM_CXX because AOCL-LibM validates those variables as existing paths.",
      "Keep AOCL-LibM out of the CPython link line. It is intended for downstream numerical libraries, not Python's own PGO run.",
      "Build testing showed libalm.so does not advertise a versioned SONAME, so the scaffold does not export it in provides()."
    ],
    "sha256sums": [
      "SKIP"
    ],
    "source_patches_replace_recipe_patches": true,
    "source_refs": [
      "aocl-libm::git+https://github.com/amd/aocl-libm-ose.git#tag=5.2.2"
    ],
    "src_subdir": "aocl-libm",
    "template": "scons-aocl-libm",
    "update_notes": [
      "When updating, audit upstream AOCL-LibM licensing and bundled notices directly from source rather than assuming the advisory aur/aocl metadata is sufficient.",
      "If AOCL-Utils packaging changes, re-check the runtime dependency and any RPATH or SONAME assumptions here.",
      "Keep the SCons compatibility carry as a source patch file instead of inline sed edits."
    ],
    "upstream_version": "5.2.2",
    "url": "https://github.com/amd/aocl-libm-ose.git"
  },
  "provenance": {
    "output_root": "packages",
    "recipe_author": "Blackcat Informatics Inc.",
    "recipe_commit": "a1d7a68",
    "recipe_date": "20260427",
    "recipe_history_count": 16,
    "recipe_repo": "https://github.com/paudley/ai-notes",
    "recipe_subdir": "strix-halo",
    "repo_subpath": "."
  },
  "recipe": {
    "branch": null,
    "depends_on": [
      "therock",
      "aocl_utils"
    ],
    "method": "scons",
    "notes": "Zen 5 optimized transcendentals (exp, log, sin, cos, pow) with\nAVX-512 codepaths for native 512-bit execution on Strix Halo.\n\nNOT linked into CPython directly: AOCL-LibM has slightly different\nULP rounding than glibc libm (cbrt(-0.0) -> +0.0, nextafter broken\nfor subnormals), which causes CPython's PGO test_math to fail.\nAvailable at runtime via LD_LIBRARY_PATH for NumPy and PyTorch.",
    "patches": [
      {
        "description": "Remove -muse-unaligned-vector-move flag. This is AOCC-only; TheRock's open-source amdclang doesn't support it. The -famd-opt flag covers equivalent optimizations.\n",
        "file": "src/SConscript",
        "marker": "muse-unaligned-vector-move",
        "sed_command": "s/ccflags.append('-muse-unaligned-vector-move')/pass  # patched: AOCC-only flag removed for amdclang/",
        "type": "sed"
      },
      {
        "description": "Add -Wno-macro-redefined to suppress CMPLX/CMPLXF macro redefinition warnings. These macros are defined in both AOCL-LibM headers and glibc complex.h.\n",
        "file": "src/SConscript",
        "marker": "Wno-macro-redefined",
        "marker_absent": true,
        "sed_command": "s/'-Werror'/'-Werror', '-Wno-macro-redefined'/",
        "type": "sed"
      },
      {
        "description": "Fix linker flags: -ealm_main is AOCC syntax, use -Wl,-e,alm_main for GCC/Clang. Add -fuse-ld=bfd because hand-written AVX assembly uses absolute relocations that lld rejects in shared libraries; GNU ld handles them via dynamic text relocations.\n",
        "file": "src/SConscript",
        "marker": "'-ealm_main'",
        "sed_command": "s|'-ealm_main'|'-Wl,-e,alm_main', '-fuse-ld=bfd'|",
        "type": "sed"
      },
      {
        "action": "add",
        "condition": "libalm.so does not contain LOCAL_PREFIX in RPATH",
        "description": "Add RPATH to libalm.so so it finds libau_cpuid.so (from AOCL-Utils) in the same directory at runtime without needing LD_LIBRARY_PATH.\n",
        "rpath": "${LOCAL_PREFIX}/lib",
        "target": "${LOCAL_PREFIX}/lib/libalm.so",
        "type": "patchelf_rpath"
      }
    ],
    "phase": "B",
    "repo": "https://github.com/amd/aocl-libm-ose.git",
    "src_dir": "aocl-libm",
    "steps": [
      7
    ]
  }
}
