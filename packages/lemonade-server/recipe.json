{
  "maintenance": {
    "advisory_references": [
      "aur/lemonade-desktop"
    ],
    "authoritative_reference": "aur/lemonade-server",
    "divergence_notes": [
      "This custom build treats the ROCm and Vulkan llama.cpp backends as packaged system-managed backends rather than Lemonade-managed runtime downloads.",
      "Carries local patches for Linux XDNA2 detection and the system-managed llama.cpp backend story that are specific to this Strix Halo packaging lane.",
      "Builds from the nisavid/lemonade fork so this package can consume local upstream fixes before they are available from the canonical Lemonade repository.",
      "Ships offline, no-fetch distro defaults and a systemd drop-in that points the Hugging Face, ModelScope, and llama.cpp model endpoints (HF_ENDPOINT, MODELSCOPE_ENDPOINT, and MODEL_ENDPOINT) at a refused loopback port, so lemond does not download models or backend executables on its own."
    ],
    "source_patches": [
      "0001-linux-npu-fallback-to-pci-id-when-accel-open-fails.patch",
      "0002-llamacpp-external-backends-are-system-managed.patch",
      "0003-remove-llamacpp-system-backend.patch",
      "0004-system-managed-llamacpp-metadata.patch"
    ],
    "update_notes": [
      "Track the nisavid/lemonade fork's main branch as the package source lane; use canonical upstream releases and AUR packages as baselines for compatibility review.",
      "Keep packaging and app/server split aligned with upstream naming changes; do not drift back toward the old lemonade-desktop era naming model.",
      "Re-test that the LEMONADE_LLAMACPP_*_BIN service overrides from the packaged llama.cpp env file still apply even when /var/lib/lemonade/config.json already exists.",
      "On 2026-05-26, adopted nisavid/lemonade fork main 13b1af25f84cf08ad5f8bf0ec58980bdfc09c9e7 so the server and app packages stay pinned to the same fork source while the app hides adapter controls for base llamacpp models.",
      "On 2026-05-27, bumped the server package release to refresh packaged system-managed llama.cpp backend metadata for the b9352 HIP and Vulkan backends.",
      "On 2026-05-28, bumped the server package release to refresh packaged system-managed llama.cpp backend metadata for the b9357 HIP and Vulkan backends.",
      "On 2026-05-31, bumped the server package release to refresh packaged system-managed llama.cpp backend metadata for the b9442 HIP and Vulkan backends.",
      "On 2026-06-15, adopted nisavid/lemonade fork main e18b9c1e352df8ab5aff2ff353402f1ec77c47f2, which syncs upstream Lemonade v10.7.0.",
      "On 2026-09-22, repinned the source to nisavid/lemonade fork main at Lemonade 11.7.0; the fork commit contains upstream v11.7.0 (2b6a7d7), and the lemonade entry in [source_pins] holds the selected commit. Refreshed patches 0002-0004 for the new base, and added the zz-secrets.conf backup entry, the offline distro defaults, and the model-endpoint drop-in. Issue 137 pins the frozen fork commit 3d5991033; issue 139 tracks the build, publish, install, and host validation.",
      "On 2026-09-23, released 11.7.0-2 with patch 0005, which makes RecipeOptions::inherit tokenize merged *_args without keeping quotes. At 3d5991033 the merge wrapped quoted values in their quote characters and then quoted them again, so llama-server received a literal '{\"preserve_thinking\":true}' for the qwen35 and qwen35moe --chat-template-kwargs architecture default and exited. The distro defaults always set --no-mmap, so every qwen35 and qwen35moe model failed to load. The regression is fork-only: fork commit e3d08ffa6 added quote_custom_arg_value to map_to_args_string, which stacks on upstream's keep_quotes=true merge (lemonade-sdk/lemonade#1920), while upstream v11.6.0, v11.7.0, and v11.9.0 produce the correct argv. The fork fix is nisavid/lemonade#168; drop patch 0005 at the Lemonade repin to a fork commit that contains #168, which is the upstream-synced fork main that the issue 141 (M6) repackage adopts. Upstream #3265 (7b5657d80, first released in v11.8.0) is context only: it moves the merge to recipe_arg_resolver.h, which still keeps quotes, so it does not fix the fork. Issues 139 and 140 track the regression.",
      "On 2026-09-25, repinned the source to the merge commit of the fork's upstream v11.9.0 sync (issue 141, nisavid/lemonade#175) and set the package version to 11.9.0. The sync contains upstream v11.9.0 (bb39eaf) and the fork fix for nisavid/lemonade#168 (2cb1a91, PR 169), so patch 0005 is dropped. Patches 0001-0004 apply unchanged to the new base with line offsets only and no fuzz; the upstream changes in the patched files do not touch the patched hunks.",
      "On 2026-09-25, followed the 11.9 service layout (issue 141): the 30-env-files.conf drop-in keeps /etc/lemonade/conf.d/*.conf loading ahead of upstream's /etc/default/lemond and loads the packaged llama.cpp env file (moved out of conf.d) last; both secrets files are backup files; no install script writes owner config; and the distro defaults gained max_loaded_models -1 and an explicit auto_update_models false. The owner's deploy steps (a sparse config.json with the host binding, and verifying the JSON migration into /var/lib/lemonade) belong to the issue 141 redeploy."
    ]
  },
  "name": "lemonade-server",
  "optional_backends": [
    "llama.cpp-hip-gfx1151",
    "llama.cpp-vulkan-gfx1151"
  ],
  "package_name": "lemonade-server",
  "pkgver": "11.9.0",
  "policy": {
    "advisory_references": [
      "aur/lemonade-desktop"
    ],
    "arch_reference": [
      "aur/lemonade-server",
      "aur/lemonade-desktop"
    ],
    "authoritative_reference": "aur/lemonade-server",
    "backup": [
      "etc/default/lemond",
      "etc/lemonade/conf.d/zz-secrets.conf"
    ],
    "baseline_kind": "closest-upstream",
    "conflicts": [],
    "depends": [
      "brotli",
      "curl",
      "glibc",
      "libcap",
      "libdrm",
      "libgcc",
      "libstdc++",
      "libwebsockets",
      "systemd-libs",
      "zlib",
      "zstd"
    ],
    "divergence_notes": [
      "This custom build treats the ROCm and Vulkan llama.cpp backends as packaged system-managed backends rather than Lemonade-managed runtime downloads.",
      "Carries local patches for Linux XDNA2 detection and the system-managed llama.cpp backend story that are specific to this Strix Halo packaging lane.",
      "Builds from the nisavid/lemonade fork so this package can consume local upstream fixes before they are available from the canonical Lemonade repository.",
      "Ships offline, no-fetch distro defaults and a systemd drop-in that points the Hugging Face, ModelScope, and llama.cpp model endpoints (HF_ENDPOINT, MODELSCOPE_ENDPOINT, and MODEL_ENDPOINT) at a refused loopback port, so lemond does not download models or backend executables on its own."
    ],
    "extra_sha256sums": [
      "84149e7e765b80de94b0058edf930ee61ca39ceee534bff4e01fdd7e050cfb27",
      "521d5a3b6d65c74dae9735ef7ca9434b1dac33f0b734e5d1208d67bc36d3ed9d",
      "6e5a0345f2b7bede467045fa12f42f4d0773a7a782b169b0009d12de1d08155d",
      "5ec6cbf9d6c06e1d4c31930060b73627591c4dfe188d234719cf3a403db319b0"
    ],
    "extra_sources": [
      "0001-linux-npu-fallback-to-pci-id-when-accel-open-fails.patch",
      "0002-llamacpp-external-backends-are-system-managed.patch",
      "0003-remove-llamacpp-system-backend.patch",
      "0004-system-managed-llamacpp-metadata.patch"
    ],
    "license": [
      "Apache-2.0"
    ],
    "makedepends": [
      "cmake",
      "git",
      "ninja",
      "nodejs",
      "npm",
      "rocm-llvm-gfx1151"
    ],
    "optdepends": [
      "llama.cpp-hip-gfx1151: llama.cpp ROCm backend",
      "llama.cpp-vulkan-gfx1151: llama.cpp Vulkan backend"
    ],
    "patches_dir": "packages/lemonade-server",
    "pkgdesc": "Local AI server runtime rebuilt for Strix Halo gfx1151",
    "pkgrel": 1,
    "provides": [
      "lemonade-server"
    ],
    "recipe_key": "lemonade",
    "repo": "https://github.com/nisavid/lemonade.git",
    "scaffold_notes": [
      "Server/runtime package; llama.cpp backends are optdepends, not hard deps.",
      "Pinned to nisavid/lemonade commit b6616eb3b6d31c3d7dbffffdb23e24adda1292a1, the merge commit of the fork's upstream v11.9.0 sync (nisavid/lemonade#175), whose CMake project version is 11.9.0. The pin is the lemonade entry in the [source_pins] table of policies/recipe-packages.toml, so lemonade-server and lemonade-app always build the same fork commit.",
      "Configures with BUILD_TESTING=OFF; upstream includes CTest, and the distro build does not need its C++ test binaries.",
      "Uses upstream's lemond.service unit name; do not ship the pre-10.3 lemonade-server.service name in this package.",
      "Uses upstream's lemond.service as installed. Since 11.9 it sets StateDirectory=lemonade (the config dir: config.json, user_models.json, recipe_options.json, jobs.json, mcp_servers.json), CacheDirectory=lemonade with mode 0755 (bin/ and aliases.json), a bare ExecStart=/usr/bin/lemond, and EnvironmentFile=-/etc/default/lemond as its only EnvironmentFile= line, and upstream installs no lemond.service.d drop-in. package() fails the build if the unit loses that layout or upstream installs any file under lemond.service.d/ (checked before the package installs its own drop-ins), because a cache-dir argument or a LEMONADE_CACHE_DIR, HOME, or XDG_* override would make the config dir follow the cache dir, and because 30-env-files.conf resets the EnvironmentFile= list and would silently drop any other upstream entry, including one in an upstream drop-in that sorts before it. On first start lemond moves the five JSON files from the legacy /var/lib/lemonade/.cache/lemonade into /var/lib/lemonade without overwriting, and the rest of that tree into /var/cache/lemonade.",
      "Installs /usr/lib/systemd/system/lemond.service.d/30-env-files.conf, which owns the service's whole EnvironmentFile= order. It first resets the list with an empty EnvironmentFile=, because systemd keeps a repeated path at its first position, so listing /etc/default/lemond again without the reset would not move it after conf.d. It then lists -/etc/lemonade/conf.d/*.conf, -/etc/default/lemond, and the required /usr/lib/lemonade/llamacpp-gfx1151.env. systemd reads them in that order and a later file overrides an earlier one, so /etc/default/lemond overrides conf.d, a key set only in conf.d still applies, and the package env file overrides any stale LEMONADE_LLAMACPP_* key in the owner files.",
      "Installs /usr/lib/lemonade/llamacpp-gfx1151.env so the packaged ROCm and Vulkan llama.cpp wrapper binaries are exposed to the service as system-managed backends. lemond reads LEMONADE_LLAMACPP_*_BIN from the environment ahead of config.json on every backend lookup, so the refreshed system-managed backend patch no longer carries the older config-load environment overlay or the CLI backend-table change.",
      "Exports the packaged llama.cpp revision and ggml release URL in the same env file so the GUI shows the packaged backend metadata instead of upstream downloader defaults. Through 11.7 these lived in /etc/lemonade/conf.d/10-llamacpp-gfx1151.conf, with the same values; the metadata patch reads them only from the environment. To override a LEMONADE_LLAMACPP_* key, add an owner drop-in under /etc/systemd/system/lemond.service.d/ that sorts after 30-env-files.conf, such as 40-llamacpp-override.conf, with its own EnvironmentFile= line, which systemd reads after the package env file. An Environment= line there does not override the key, because every EnvironmentFile= value overrides Environment=.",
      "The system-managed backend patch probes llama-server --version through Lemonade's argv-based ProcessManager capture path, not a shell.",
      "Marks upstream's /etc/default/lemond (mode 0640, the service's HF_TOKEN, LEMONADE_API_KEY, and LEMONADE_ADMIN_API_KEY file) and /etc/lemonade/conf.d/zz-secrets.conf (mode 0660) as backup files so an upgrade keeps local edits. Upstream 11.9 no longer installs zz-secrets.conf, so the package ships 11.7's placeholder byte for byte: an owner-edited copy stays in place without a .pacnew and keeps loading through the conf.d glob. If the package dropped it, pacman would rename an edited copy to zz-secrets.conf.pacsave, which the glob does not match. The llama.cpp env file stays package-owned so each release refreshes the backend metadata.",
      "No install script writes owner config: nothing is merged into /etc/default/lemond. Moving settings from conf.d into /etc/default/lemond is optional, because conf.d keeps loading. Since 11.9, LEMONADE_* settings in either file only seed a config.json that does not exist yet, so server settings such as the host binding belong in config.json.",
      "Replaces upstream's full /usr/share/lemonade/defaults.json with a sparse distro overlay: offline, no_fetch_executables, auto_update_models off, max_loaded_models -1 (unlimited), an explicit ROCm llama.cpp backend, prefer_system off, the packaged rocm_bin and vulkan_bin paths, and --no-mmap llama.cpp args. With --no-mmap set, lemond omits its iGPU --load-mode default, which the packaged llama.cpp b9442 does not accept. Pins count toward residency, so a slot limit refuses every new LLM load once the pins fill it; the GTT-aware occupancy budget is the admission bound. lemond merges the overlay over its built-in defaults and under config.json, so keys an existing config.json already sets still win, and since 11.9 every config write prunes config.json keys that equal the merged defaults. The overlay leaves host, port, broadcast, and API-key settings to the host; since 11.9, --host and --port are runtime-only and LEMONADE_* variables only seed a new config.json, so the host binding belongs in config.json.",
      "Installs /usr/lib/systemd/system/lemond.service.d/20-no-remote-model-fetch.conf, which sets HF_ENDPOINT, MODELSCOPE_ENDPOINT, and MODEL_ENDPOINT to a refused loopback port. lemond's model registry reads HF_ENDPOINT and MODELSCOPE_ENDPOINT on every request, and llama-server children inherit all three. MODEL_ENDPOINT is required because llama-server reads it ahead of HF_ENDPOINT, so an inherited MODEL_ENDPOINT would otherwise bypass the HF_ENDPOINT blackhole. The drop-in stays Environment=, so an owner env file (conf.d or /etc/default/lemond) that sets one of these keys overrides it, because systemd applies EnvironmentFile after Environment; that override is an explicit owner act, not a silent downloader.",
      "Removes /usr/share/metainfo because the web-app MetaInfo names lemonade-web-app.desktop as its launchable, and this package removes that desktop entry."
    ],
    "sha256sums": [
      "SKIP"
    ],
    "source_refs": [
      "lemonade::git+https://github.com/nisavid/lemonade.git#commit=b6616eb3b6d31c3d7dbffffdb23e24adda1292a1"
    ],
    "src_subdir": "lemonade",
    "template": "lemonade-server",
    "update_notes": [
      "Track the nisavid/lemonade fork's main branch as the package source lane; use canonical upstream releases and AUR packages as baselines for compatibility review.",
      "Keep packaging and app/server split aligned with upstream naming changes; do not drift back toward the old lemonade-desktop era naming model.",
      "Re-test that the LEMONADE_LLAMACPP_*_BIN service overrides from the packaged llama.cpp env file still apply even when /var/lib/lemonade/config.json already exists.",
      "On 2026-05-26, adopted nisavid/lemonade fork main 13b1af25f84cf08ad5f8bf0ec58980bdfc09c9e7 so the server and app packages stay pinned to the same fork source while the app hides adapter controls for base llamacpp models.",
      "On 2026-05-27, bumped the server package release to refresh packaged system-managed llama.cpp backend metadata for the b9352 HIP and Vulkan backends.",
      "On 2026-05-28, bumped the server package release to refresh packaged system-managed llama.cpp backend metadata for the b9357 HIP and Vulkan backends.",
      "On 2026-05-31, bumped the server package release to refresh packaged system-managed llama.cpp backend metadata for the b9442 HIP and Vulkan backends.",
      "On 2026-06-15, adopted nisavid/lemonade fork main e18b9c1e352df8ab5aff2ff353402f1ec77c47f2, which syncs upstream Lemonade v10.7.0.",
      "On 2026-09-22, repinned the source to nisavid/lemonade fork main at Lemonade 11.7.0; the fork commit contains upstream v11.7.0 (2b6a7d7), and the lemonade entry in [source_pins] holds the selected commit. Refreshed patches 0002-0004 for the new base, and added the zz-secrets.conf backup entry, the offline distro defaults, and the model-endpoint drop-in. Issue 137 pins the frozen fork commit 3d5991033; issue 139 tracks the build, publish, install, and host validation.",
      "On 2026-09-23, released 11.7.0-2 with patch 0005, which makes RecipeOptions::inherit tokenize merged *_args without keeping quotes. At 3d5991033 the merge wrapped quoted values in their quote characters and then quoted them again, so llama-server received a literal '{\"preserve_thinking\":true}' for the qwen35 and qwen35moe --chat-template-kwargs architecture default and exited. The distro defaults always set --no-mmap, so every qwen35 and qwen35moe model failed to load. The regression is fork-only: fork commit e3d08ffa6 added quote_custom_arg_value to map_to_args_string, which stacks on upstream's keep_quotes=true merge (lemonade-sdk/lemonade#1920), while upstream v11.6.0, v11.7.0, and v11.9.0 produce the correct argv. The fork fix is nisavid/lemonade#168; drop patch 0005 at the Lemonade repin to a fork commit that contains #168, which is the upstream-synced fork main that the issue 141 (M6) repackage adopts. Upstream #3265 (7b5657d80, first released in v11.8.0) is context only: it moves the merge to recipe_arg_resolver.h, which still keeps quotes, so it does not fix the fork. Issues 139 and 140 track the regression.",
      "On 2026-09-25, repinned the source to the merge commit of the fork's upstream v11.9.0 sync (issue 141, nisavid/lemonade#175) and set the package version to 11.9.0. The sync contains upstream v11.9.0 (bb39eaf) and the fork fix for nisavid/lemonade#168 (2cb1a91, PR 169), so patch 0005 is dropped. Patches 0001-0004 apply unchanged to the new base with line offsets only and no fuzz; the upstream changes in the patched files do not touch the patched hunks.",
      "On 2026-09-25, followed the 11.9 service layout (issue 141): the 30-env-files.conf drop-in keeps /etc/lemonade/conf.d/*.conf loading ahead of upstream's /etc/default/lemond and loads the packaged llama.cpp env file (moved out of conf.d) last; both secrets files are backup files; no install script writes owner config; and the distro defaults gained max_loaded_models -1 and an explicit auto_update_models false. The owner's deploy steps (a sparse config.json with the host binding, and verifying the JSON migration into /var/lib/lemonade) belong to the issue 141 redeploy."
    ],
    "upstream_name": "lemonade-server",
    "upstream_version": "11.9.0",
    "url": "https://github.com/nisavid/lemonade"
  },
  "provenance": {
    "output_root": "packages",
    "recipe_author": "Blackcat Informatics Inc.",
    "recipe_commit": "3f15f9f",
    "recipe_date": "20260508",
    "recipe_history_count": 17,
    "recipe_repo": "https://github.com/paudley/ai-notes",
    "recipe_subdir": "strix-halo",
    "repo_subpath": "."
  },
  "recipe": {
    "branch": "main",
    "depends_on": [
      "therock",
      "llamacpp"
    ],
    "method": "pip",
    "notes": "Lemonade is a unified inference server wrapping llama.cpp (GPU/CPU),\nFLM (NPU), and ONNX backends behind an OpenAI-compatible API.\n\nThe project split in v10: the git repo (v10.0.0) is a C++ server,\nwhile the Python SDK is published separately as lemonade-sdk on PyPI\n(v9.1.4). The SDK handles llama-server process management, model\ndownloads, .env loading, and hardware detection.\n\nVersion pin fixes: lemonade-sdk pins huggingface-hub==0.33.0,\nonnx==1.18.0, transformers<=4.53.2 which conflict with vLLM.\nReinstalling at compatible versions resolves conflicts.",
    "patches": [],
    "phase": "I",
    "repo": "https://github.com/lemonade-sdk/lemonade.git",
    "src_dir": "lemonade",
    "steps": [
      34,
      35,
      36
    ]
  },
  "role": "server-runtime"
}
