{
  "maintenance": {
    "advisory_references": [
      "extra/python-pytorch-rocm",
      "cachyos-extra-znver4/python-pytorch-opt-rocm"
    ],
    "authoritative_reference": "extra/python-pytorch-opt-rocm",
    "divergence_notes": [
      "Uses the Arch split-package structure as the integration baseline but deliberately tracks ROCm/pytorch release/2.11 rather than upstream pytorch/pytorch.",
      "Carries recipe-specific ROCm fixes such as the HIPGraph stub rewrite, gfx1151 CK enablement, and post-install patchelf/linker cleanup.",
      "Pins PyTorch's BLAS/LAPACK provider to OpenBLAS and uses a two-stage wheel assembly flow on Arch Python 3.14 to avoid host oneMKL contamination and the broken install target that mirrors /usr/lib into the source tree."
    ],
    "source_patches": [
      "0001-setup-allow-skipping-build-deps.patch",
      "0002-use-wide-magma-version-encoding.patch"
    ],
    "update_notes": [
      "When updating, inspect the current Arch python-pytorch pkgbase first, then re-evaluate every carried recipe/source patch against the chosen ROCm fork.",
      "On 2026-04-22, reviewed ROCm/pytorch release/2.11 through 141ba657575b42e5d0869002b509af4a75899edc. The reviewed range only changed Windows int4mm device memcpy handling and FP64 hipBLASLt TunableOp support, with no overlap against the local Python 3.14 wheel flow, HIPGraph stub, NumPy target define, MAGMA version encoding, gfx1151 CK enablement, or BLAS/provider carry. Record the reviewed branch head without repinning the package source until a local workload needs those fixes or a broader PyTorch rebuild lane is already open.",
      "Keep the package version aligned with the built wheel version; do not repeat the earlier mismatch where the package claimed 2.11.0 but the built wheel came from develop.",
      "Keep openblas explicit in both depends and makedepends so the build does not drift back to generic host BLAS auto-detection.",
      "Preserve the current Arch Python 3.14 wheel flow unless upstream changes materially: build CMake artifacts first, tolerate the known _sysconfigdata__linux_x86_64-linux-gnu.cpython-314.pyc install failure, run the safe CMake install scripts that stage PyTorch C++ extension headers and Caffe2 package metadata, restage the built torch/lib and torch/bin payloads, then run SKIP_BUILD_DEPS=1 python setup.py bdist_wheel."
    ]
  },
  "name": "python-pytorch-opt-rocm-gfx1151",
  "package_name": "python-pytorch-opt-rocm-gfx1151",
  "pkgver": "2.11.0",
  "policy": {
    "advisory_references": [
      "extra/python-pytorch-rocm",
      "cachyos-extra-znver4/python-pytorch-opt-rocm"
    ],
    "arch_reference": [
      "extra/python-pytorch-opt-rocm",
      "extra/python-pytorch-rocm",
      "cachyos-extra-znver4/python-pytorch-opt-rocm"
    ],
    "authoritative_reference": "extra/python-pytorch-opt-rocm",
    "conflicts": [
      "python-pytorch-opt-rocm",
      "python-pytorch-rocm",
      "python-pytorch"
    ],
    "depends": [
      "amdsmi-gfx1151",
      "composable-kernel-gfx1151",
      "hsa-amd-aqlprofile-gfx1151",
      "hipblaslt-gfx1151",
      "miopen-hip-gfx1151",
      "openblas",
      "python-aotriton-gfx1151",
      "python-filelock",
      "python-fsspec",
      "python-gfx1151",
      "python-jinja",
      "python-networkx",
      "python-numpy-gfx1151",
      "python-sympy",
      "python-typing_extensions",
      "python-yaml",
      "rccl-gfx1151",
      "rocblas-gfx1151",
      "rocm-hip-sdk-gfx1151",
      "rocm-openmp-gfx1151"
    ],
    "divergence_notes": [
      "Uses the Arch split-package structure as the integration baseline but deliberately tracks ROCm/pytorch release/2.11 rather than upstream pytorch/pytorch.",
      "Carries recipe-specific ROCm fixes such as the HIPGraph stub rewrite, gfx1151 CK enablement, and post-install patchelf/linker cleanup.",
      "Pins PyTorch's BLAS/LAPACK provider to OpenBLAS and uses a two-stage wheel assembly flow on Arch Python 3.14 to avoid host oneMKL contamination and the broken install target that mirrors /usr/lib into the source tree."
    ],
    "extra_sha256sums": [
      "95a46381bedcff873f9765c9b103877aa36f649e649c5b223582732b2ee61cac",
      "944e0fb4db31da678993acd3104709083ef28dcee58fcc24ab1675d880c39846"
    ],
    "license": [
      "BSD-3-Clause-Modification"
    ],
    "makedepends": [
      "cmake",
      "git",
      "ninja",
      "openblas",
      "python-build",
      "python-cffi",
      "python-installer",
      "python-numpy-gfx1151",
      "python-pip",
      "python-requests",
      "python-setuptools",
      "python-six",
      "python-triton-gfx1151",
      "python-yaml",
      "patchelf",
      "rocm-llvm-gfx1151",
      "unzip"
    ],
    "patches_dir": "patches/python-pytorch-opt-rocm-gfx1151",
    "pkgdesc": "Tensors and dynamic neural networks in Python with ROCm acceleration, rebuilt for Strix Halo gfx1151",
    "provides": [
      "python-pytorch-opt-rocm=2.11.0",
      "python-pytorch-rocm=2.11.0",
      "python-pytorch=2.11.0"
    ],
    "recipe_key": "pytorch",
    "scaffold_notes": [
      "Authoritative base: Arch split package python-pytorch-opt-rocm 2.11.0-3, with python-pytorch-rocm kept as an advisory sibling because the same pkgbase carries both variants.",
      "The recipe intentionally tracks the ROCm/pytorch fork rather than upstream pytorch/pytorch because AMD validates that branch and carries ROCm-specific integration fixes not yet upstreamed.",
      "Keep nearby Cachy packaging as advisory input for compiler defaults and dependency polish, but use the Arch split-package structure as the distro-integration baseline.",
      "The first real build must preserve the recipe's ROCm-specific fixes: HIPGraph.hip stub rewrite, NumPy 2 target C-API define, clang ABI flag removal, gfx1151 CK enablement, and post-install patchelf fixes for torch/lib and libtorch_hip.so.",
      "The MAGMA version-encoding fix is carried as a source patch on the canonical CUDA linalg input; ROCm hipify derives the HIP source from that patched input during build.",
      "Use OpenBLAS explicitly for this lane. Letting the build auto-detect host oneMKL produced a broken wheel with /opt/intel/oneapi runpaths and NumPy import failures.",
      "On Arch Python 3.14, the CMake install target currently mirrors /usr/lib and /usr/include into the source tree and then fails on a root-owned _sysconfigdata pyc. The maintained workaround is to build first, accept that known install failure, run the safe header/CMake metadata install scripts, restage the built torch/lib and torch/bin artifacts, and assemble the wheel with SKIP_BUILD_DEPS=1.",
      "After the known CMake install-target failure, the package must still run the torch/headeronly, c10, and caffe2 install scripts before wheel assembly so the wheel ships ATen, c10, torch/csrc, and CMake metadata for downstream extension builds.",
      "The SKIP_BUILD_DEPS source patch must apply before wheel assembly so bdist_wheel uses the already staged artifacts instead of re-running the CMake install target."
    ],
    "source_patches": [
      "0001-setup-allow-skipping-build-deps.patch",
      "0002-use-wide-magma-version-encoding.patch"
    ],
    "source_refs": [
      "pytorch::git+https://github.com/ROCm/pytorch.git#commit=8543095e3275db694084a6679bd5b61f7d2ece76"
    ],
    "src_subdir": "pytorch",
    "template": "python-project-pytorch-rocm",
    "update_notes": [
      "When updating, inspect the current Arch python-pytorch pkgbase first, then re-evaluate every carried recipe/source patch against the chosen ROCm fork.",
      "On 2026-04-22, reviewed ROCm/pytorch release/2.11 through 141ba657575b42e5d0869002b509af4a75899edc. The reviewed range only changed Windows int4mm device memcpy handling and FP64 hipBLASLt TunableOp support, with no overlap against the local Python 3.14 wheel flow, HIPGraph stub, NumPy target define, MAGMA version encoding, gfx1151 CK enablement, or BLAS/provider carry. Record the reviewed branch head without repinning the package source until a local workload needs those fixes or a broader PyTorch rebuild lane is already open.",
      "Keep the package version aligned with the built wheel version; do not repeat the earlier mismatch where the package claimed 2.11.0 but the built wheel came from develop.",
      "Keep openblas explicit in both depends and makedepends so the build does not drift back to generic host BLAS auto-detection.",
      "Preserve the current Arch Python 3.14 wheel flow unless upstream changes materially: build CMake artifacts first, tolerate the known _sysconfigdata__linux_x86_64-linux-gnu.cpython-314.pyc install failure, run the safe CMake install scripts that stage PyTorch C++ extension headers and Caffe2 package metadata, restage the built torch/lib and torch/bin payloads, then run SKIP_BUILD_DEPS=1 python setup.py bdist_wheel."
    ],
    "upstream_version": "2.11.0",
    "url": "https://pytorch.org"
  },
  "provenance": {
    "output_root": "packages",
    "recipe_author": "Blackcat Informatics Inc.",
    "recipe_commit": "ad42886",
    "recipe_date": "20260317",
    "recipe_history_count": 8,
    "recipe_repo": "https://github.com/paudley/ai-notes",
    "recipe_subdir": "strix-halo",
    "repo_subpath": "."
  },
  "recipe": {
    "branch": "develop",
    "depends_on": [
      "therock",
      "cpython"
    ],
    "method": "pip",
    "notes": "ROCm fork carries AMD-specific fixes (hipify, Tensile, rocm_smi\nlinkage) not yet upstreamed. The upstream pytorch/pytorch works with\nUSE_ROCM=1 but AMD's CI tests against this fork.\n\nUSE_ROCM_CK_GEMM=ON enables Composable Kernel GEMM for ROCm.",
    "patches": [
      {
        "description": "Set NPY_TARGET_VERSION to numpy 2.0 C-API (0x12). Without this, numpy 2.x headers compile against oldest compatible API (1.20), producing a .so that crashes at import with numpy 2.x installed.\n",
        "file": "torch/csrc/utils/numpy_stub.h",
        "marker": "NPY_TARGET_VERSION",
        "marker_absent": true,
        "sed_command": "s|#include <numpy/arrayobject.h>|// Target numpy 2.0 C-API (0x12) for ABI compatibility with numpy >= 2.0.\\n#ifndef NPY_TARGET_VERSION\\n#define NPY_TARGET_VERSION 0x00000012\\n#endif\\n\\n#include <numpy/arrayobject.h>|",
        "type": "sed"
      },
      {
        "description": "Remove -fclang-abi-compat=17 from HIPCC flags. PyTorch adds it for compat with newer hip-clang C++20 mangling rules, but it forces HIP device code to use Clang 17 ABI while host code uses amdclang 22 ABI, causing undefined symbol errors.\n",
        "file": "cmake/Dependencies.cmake",
        "marker": "fclang-abi-compat=17",
        "sed_command": "s/list(APPEND HIP_HIPCC_FLAGS -fclang-abi-compat=17)/# Removed: causes ABI mismatch with host amdclang 22/",
        "type": "sed"
      },
      {
        "description": "Add gfx1151 to Composable Kernel GEMM supported architectures. Without this, PyTorch logs \"Attempting to use CK on an unsupported architecture!\" and TunableOp cannot include CK kernels in its autotuning candidates. The gate is a hardcoded vector of arch strings.\n",
        "file": "aten/src/ATen/Context.cpp",
        "marker": "gfx1151",
        "marker_absent": true,
        "sed_command": "s/\"gfx90a\", \"gfx942\", \"gfx950\"/\"gfx90a\", \"gfx942\", \"gfx950\", \"gfx1151\"/",
        "type": "sed"
      },
      {
        "description": "Replace HIPGraph.hip entirely. hipify creates a set_conditional_handle() function referencing cudaGraphConditionalHandle (CUDA 12.4+ type with no HIP equivalent). The header doesn't declare it, nothing calls it on ROCm -- dead code that fails to compile. Replaced with minimal stub containing only the namespace declaration.\n",
        "file": "aten/src/ATen/hip/HIPGraph.hip",
        "marker": "cudaGraphConditionalHandle",
        "note": "Content is a heredoc in build-vllm.sh (too complex for YAML inline)",
        "type": "file_rewrite"
      },
      {
        "action": "set",
        "condition": "RUNPATH contains 'pytorch/build'",
        "description": "Fix RPATHs inside PyTorch wheel. cmake bakes build tree path (e.g. /opt/src/vllm/pytorch/build/lib) into RUNPATH, causing the dynamic linker to load unpatched .so files from the build tree instead of the wheel's copies.\n",
        "rpath": "${LOCAL_PREFIX}/lib:$ORIGIN",
        "target": "torch/lib/lib*.so",
        "type": "patchelf_rpath"
      },
      {
        "action": "add",
        "condition": "NEEDED contains libalm.so, libamdhip64, or librocm_smi",
        "description": "Add ROCm lib path to .so files that dynamically link ROCm libraries (libalm.so, libamdhip64, librocm_smi64) so they resolve without LD_LIBRARY_PATH at runtime.\n",
        "rpath": "${LOCAL_PREFIX}/lib",
        "target": "torch/lib/lib*.so",
        "type": "patchelf_rpath"
      },
      {
        "action": "set",
        "condition": "RUNPATH contains 'pytorch/build'",
        "description": "Fix the _C extension module RPATH. Same build tree path issue as torch/lib/*.so but needs $ORIGIN/lib to find sibling .so files in the wheel's lib/ subdirectory.\n",
        "rpath": "${LOCAL_PREFIX}/lib:$ORIGIN/lib",
        "target": "torch/_C*.so",
        "type": "patchelf_rpath"
      },
      {
        "condition": "NEEDED does not contain librocm_smi64",
        "description": "Add librocm_smi64.so to libtorch_hip.so NEEDED list. PyTorch's build system omits it from the link line despite using rsmi_* symbols -- upstream bug that causes \"undefined symbol: rsmi_init\" at runtime.\n",
        "library": "librocm_smi64.so",
        "target": "torch/lib/libtorch_hip.so",
        "type": "patchelf_needed"
      }
    ],
    "phase": "C",
    "repo": "https://github.com/ROCm/pytorch.git",
    "src_dir": "pytorch",
    "steps": [
      9,
      10,
      11
    ]
  }
}
